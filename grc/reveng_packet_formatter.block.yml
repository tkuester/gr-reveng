# auto-generated by grc.converter

id: reveng_packet_formatter
label: Packet Formatter
category: reveng

parameters:
-   id: format_str
    label: Format String
    dtype: string
-   id: file_name
    label: File Name
    category: Logging
    dtype: string
    hide: ${ ('none' if len(file_name) > 0 else 'part') }
-   id: log_to
    label: Options
    category: Logging
    dtype: enum
    default: stdout
    options: ['"stdout"', '"file"', '"stdout_file"']
    option_labels: [stdout, File, stdout + File]
    hide: part
-   id: append
    label: Append
    category: Logging
    dtype: enum
    default: 'False'
    options: ['True', 'False']
    hide: part
-   id: flush
    label: Flush Every Packet
    category: Logging
    dtype: enum
    default: 'True'
    options: ['True', 'False']
    hide: part

inputs:
-   domain: message
    id: pdus

templates:
    imports: import reveng
    make: reveng.packet_formatter(format_str=${format_str}, file_name=${file_name},
        log_to=${log_to}, append=${append}, flush=${flush})

file_format: 1
