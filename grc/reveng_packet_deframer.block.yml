# auto-generated by grc.converter

id: reveng_packet_deframer
label: Packet Deframer
category: reveng

parameters:
-   id: name
    label: Name
    dtype: string
    hide: ${ ('none' if len(name) > 0 else 'part') }
-   id: sync
    label: Sync Word
    dtype: raw
    default: map(int, bin(0xd391)[2:].zfill(16))
-   id: fixed_len
    label: Mode
    dtype: enum
    options: ['True', 'False']
    option_labels: [Fixed Length, Variable Length]
-   id: pkt_len
    label: Pkt Len (bits)
    dtype: int
    default: '32'
    hide: ${ ('none' if str(fixed_len).startswith('True') else 'all') }
-   id: max_len
    label: Max Len (bytes)
    dtype: int
    default: '0'
    hide: ${ ((str(fixed_len).startswith('False')) and ((max_len > 0) and 'none' or
        'part')) or 'all' }
-   id: pkt_len_offset
    label: Len Offset (bytes)
    dtype: int
    default: '0'
    hide: ${ ('all' if str(fixed_len).startswith('True') else 'none') }
-   id: pkt_len_adj
    label: Additional Bytes
    dtype: int
    default: '0'
    hide: ${ ((str(fixed_len).startswith('False')) and ((pkt_len_adj > 0) and 'none'
        or 'part')) or 'all' }
-   id: encoding
    label: Encoding
    category: Data Format
    dtype: enum
    options: [none]
    hide: ${ ('part' if (encoding).startswith('none') else 'none') }
-   id: msb
    label: MSB First
    category: Data Format
    dtype: enum
    default: 'True'
    options: ['True']
    option_labels: ['Yes']
    hide: part
-   id: big_endian
    label: Endianness
    category: Data Format
    dtype: enum
    default: 'True'
    options: ['True']
    option_labels: [Big Endian]
    hide: part
-   id: pack_bytes
    label: Pack Bytes
    category: Data Format
    dtype: enum
    default: 'False'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
    hide: part

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: message
    id: out
    optional: true

templates:
    imports: import reveng
    make: reveng.packet_deframer(${name}, ${sync}, ${fixed_len}, ${pkt_len}, ${max_len},
        ${pkt_len_offset}, ${pkt_len_adj}, ${pack_bytes})

file_format: 1
