# auto-generated by grc.converter

id: reveng_pattern_dump
label: Pattern Dump
category: reveng
flags:
- deprecated

parameters:
-   id: pattern
    label: Pattern
    dtype: raw
    default: '[0,1]*16'
-   id: dump_len
    label: Dump Length
    dtype: int
    default: '32'
-   id: output_fmt
    label: Output Format
    dtype: string
    default: '%Y-%m-%d %H:%M:%S,%[bits]'
-   id: rel_time
    label: Time Reference
    dtype: enum
    options: ['False', 'True', None]
    option_labels: [Absolute, Relative, No Time]
-   id: file_name
    label: File Output
    dtype: string
-   id: stdout
    label: Print to stdout
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: message
    id: out
    optional: true

templates:
    imports: import reveng
    make: reveng.pattern_dump(${pattern}, ${dump_len}, ${output_fmt}, ${rel_time},
        ${file_name}, ${stdout})

file_format: 1
